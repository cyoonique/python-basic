import os
import sys

#
# Complete the miniMaxSum function below.
#
def miniMaxSum(arr):
    totalsum = sum(arr)
    print(totalsum-(max(arr)), totalsum - (min(arr)))
           
if __name__ == '__main__':
    arr = list(map(int, input().rstrip().split()))

    miniMaxSum(arr)
