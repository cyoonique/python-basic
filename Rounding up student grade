import os
import sys


def gradingStudents(grades): 
    # Complete this function
    for grade in grades:
        if grade >= 38:
            if grade % 5 > 2:
                grade += 5 - (grade % 5)
        print(grade)
        
grades = [73, 67, 38, 33]
gradingStudents(grades)
